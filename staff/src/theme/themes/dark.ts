import { CreateTheme } from '..'


export const createDarkTheme: CreateTheme = ({ theme, colorScheme }) => {
    if (colorScheme !== 'dark') return;

    /** Define Dark Theme overrides
     * @see {@link https://mantine.dev/theming/theme-object/}
     */
    return {

    }
}
