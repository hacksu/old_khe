import { CreateTheme } from '../index'


export const createLightTheme: CreateTheme = ({ theme, colorScheme }) => {
    if (colorScheme !== 'light') return;

    /** Define Light Theme overrides
     * @see {@link https://mantine.dev/theming/theme-object/}
     */
    return {
        
    }
}
