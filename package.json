{
  "name": "khe",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "api",
    "staff",
    "app"
  ],
  "scripts": {
    "dev": "cross-env FORCE_COLOR=1 turbo run dev --parallel",
    "start": "cross-env FORCE_COLOR=1 turbo run start --parallel",
    "production": "cross-env FORCE_COLOR=1 turbo run production --parallel",
    "build": "cross-env FORCE_COLOR=1 turbo run build",
    "lint": "cross-env FORCE_COLOR=1 turbo run lint",
    "deploy": "cross-env FORCE_COLOR=1 turbo run deploy",
    "exports": "exports"
  },
  "devDependencies": {
    "@cseitz/exports": "^1.0.8",
    "cross-env": "^7.0.3",
    "turbo": "latest",
    "yarn": "^1.22.19"
  },
  "packageManager": "yarn@1.22.19",
  "engines": {
    "npm": ">=7.0.0",
    "turbo": ">=14.0.0"
  },
  "author": "Chris Seitz <54898870+cseitz@users.noreply.github.com>"
}
